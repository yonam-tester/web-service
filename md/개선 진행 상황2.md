# 📌 연암 테스터 기능 개선 진행 상황2 (Step 7)

본 문서는 '개선 테스크2.md' 명세서를 기반으로 하여 실제 소스코드에 적용된 기능 개선 및 보완 태스크의 진행 현황과 테스트 검증 결과를 기록하는 문서입니다. **모든 보완 구현은 md 폴더 밑의 명세서 기준을 엄격하게 준수하였습니다.**

---

## 📅 Step 1: 불필요한 UI 요소 제거 - 완료

### 🛠️ 구현된 테스크 상세 목록

#### [Task 1.1.1] 우측 상단 헤더 UI 제거 (완료)
* **구현 내용:**
  - `App.tsx` 내 `NavigationWrapper` 컴포넌트의 `<header>` 영역에서 알림 버튼(bell 아이콘), 설정 버튼(cog 아이콘), 사용자 프로필 아바타 이미지 컨테이너 블록이 정상적으로 미노출되고 있음을 확인 완료 (이전 상태에서 이미 정돈됨).

#### [Task 1.1.2] 좌측 사이드바 UI 제거 (완료)
* **구현 내용:**
  - `App.tsx` 내 `<aside>` 태그 영역에서 `QA TERMINAL` 타이틀 텍스트, `v2.4.0-stable` 정보 라벨을 삭제하였습니다.
  - `Reports` 네비게이션 링크, `API Keys` 네비게이션 링크, `Upgrade Plan` 버튼, `Support` 및 `Logout` 링크를 영구 삭제 완료하였습니다.
  - 이를 통해 좌측 사이드바에는 오직 **`Projects`**와 **`Documentation`** 2개의 필수 탭만 남아 간결하고 가독성 높은 인터페이스로 정돈되었습니다.

---

## 🧪 Step 1 컴파일 및 빌드 검증 결과

* **테스트 수행 코드:** 프론트엔드 React 소스 빌드 테스트
* **테스트 실행 명령:**
  ```bash
  npm run build
  ```
* **테스트 결과:** **`BUILD SUCCESS`** (`tsc && vite build` 컴파일 타입 체크 및 번들링 무결점 완료)

---

## 📘 Step 1 사용자 테스트 가이드

실제 사용자가 이 단계를 직접 확인하고 동작을 테스트할 수 있는 단계별 가이드입니다.

### 1. 브라우저 대시보드 화면 확인 (E2E)
1. 프론트엔드 React 개발 서버(`http://localhost:5173`) 및 백엔드를 가동합니다.
2. 웹 브라우저를 통해 `http://localhost:5173/dashboard`로 이동합니다.
3. 좌측 내비게이션 바에 오직 **`Projects`**와 **`Documentation`** 2개의 활성 탭만 남아있고, `QA TERMINAL`, `v2.4.0-stable`, `Reports`, `API Keys`, `Upgrade Plan`, `Support`, `Logout` 버튼이나 텍스트가 노출되지 않는지 시각적으로 검증합니다.
4. 우측 상단 헤더 영역에 알림 버튼(벨), 설정 버튼(톱니바퀴), 프로필 아바타 없이 오직 **`새 프로젝트 생성`** 버튼과 대시보드/Documentation/Support 링크만 깨끗하게 잔존하는지 최종 확인합니다.

---

## 📅 Step 2: 분석 대상 파일 선택 기능 및 QA 관점 고도화 - 완료

### 🛠️ 구현된 테스크 상세 목록

#### [Task 2.1.1] 문서 다중 선택 UI 이식 및 API 규격 연동 (완료)
* **구현 내용:**
  - `DocumentUploadPage.tsx` 내에 `selectedDocIds` 상태 변수를 도입하였습니다.
  - 업로드 완료된 문서 목록(`status === 'DONE'`)의 좌측에 개별 다중 선택 체크박스를 제공하여 원하는 문서만 골라 분석할 수 있도록 UI를 개선하였습니다.
  - 분석 시작 버튼 클릭 시, 전체 파일 대신 `selectedDocIds` 배열을 `targetDocumentIds` 파라미터에 실어 `POST /api/projects/{projectId}/analysis` API를 요청하도록 연동을 보장하였습니다.
  - 문서가 하나도 선택되지 않은 상태에서는 분석을 시작하지 못하도록 예외 처리를 적용하였습니다.

#### [Task 2.1.2] QA 관점 상세 템플릿 연동 (완료)
* **구현 내용:**
  - `AnalysisService.java` 내에서 분석 트리거 시 활성화된 QA 관점(`SECURITY`, `PERFORMANCE`, `BACKEND`, `FRONTEND`, `API` 등)별로 지정된 전문 검증 가이드 프롬프트를 자동으로 조립하는 로직을 추가하였습니다.
  - 조립된 가이드는 사용자 커스텀 프롬프트 앞부분에 `[QA 검증 관점 가이드]` 형태로 자동 병합되어 LLM 분석 작업 정보 및 외부 RAG/FastAPI 서버로 일치하여 전송됩니다.

---

## 🧪 Step 2 JUnit 통합 테스트 검증 결과

* **테스트 수행 코드:** [Phase6ExtensionsTests.java](file:///c:/capd/yeonam_tester/backend/src/test/java/com/yeonam/tester/Phase6ExtensionsTests.java)
* **테스트 실행 명령:**
  ```bash
  .maven\apache-maven-3.9.6\bin\mvn.cmd clean test -Dtest=Phase6ExtensionsTests
  ```
* **테스트 시나리오 및 결과:**
  - `testStartAnalysisWithTargetDocumentsAndQaPerspectives()`:
    1. H2 DB 상에 더미 프로젝트와 업로드된 문서 2개(`docA`, `docB`) 생성.
    2. 다중 관점(`SECURITY`, `PERFORMANCE`)과 문서 A만 타겟하여 `analysisService.startAnalysis` 호출.
    3. H2 DB에 저장된 `AnalysisJob` 레코드의 `customPrompt` 필드를 조회하여, 각 관점의 검증 가이드 문장과 기존 프롬프트가 동적으로 올바르게 병합되어 보관되었는지 최종 검증.
  - **테스트 결과:** **`BUILD SUCCESS`** (`Tests run: 5, Failures: 0, Errors: 0, Skipped: 0`)

---

## 📘 Step 2 사용자 테스트 가이드

실제 사용자가 이 단계를 직접 확인하고 동작을 테스트할 수 있는 단계별 가이드입니다.

### 1. 웹 브라우저 다중 선택 및 관점 테스트 (E2E)
1. 프론트엔드 React 개발 서버(`http://localhost:5173`)와 백엔드 서버(8080 포트)를 실행합니다.
2. `http://localhost:5173/upload-demo` 페이지에 진입하여 문서를 2개 이상 업로드합니다.
3. 문서 업로드가 완료(`status = DONE`)되면 각 문서 왼쪽에 **체크박스**가 활성화됩니다.
4. 그 중 **1개의 문서만 체크**하고, 우측의 QA 관점에서 **`SECURITY` (보안) 관점만 체크**합니다.
5. '커스텀 AI 프롬프트' 영역에 임의의 지시 문구(예: "추가 요망")를 입력하고 **'분석 시작'**을 누릅니다.
6. H2 DB 콘솔(`http://localhost:8080/h2-console`)에 접속하여 `SELECT * FROM ANALYSIS_JOB;` 쿼리를 실행합니다.
7. 생성된 작업 레코드의 `CUSTOM_PROMPT`가 보안 전문 가이드를 포함하여 `"SECURITY: 인증, 인가, 데이터 오용, 입력값 유효성 검증을 위한 침투형 시나리오 수립에 집중하라."`로 시작하고, 내가 입력한 프롬프트가 이어서 들어와 있는지 눈으로 직접 검증합니다.

---

## 📅 Step 3: LLM Server의 파일 참조 및 근거(Evidence) 데이터 매핑 - 완료

### 🛠️ 구현된 테스크 상세 목록

#### [Task 3.1.1] LLM 서버의 파일 파싱 및 참조 및 결과 포맷 근거(Evidence) 요구 강화 (완료)
* **구현 내용:**
  - `llm_server/document_parser.py`는 수신한 `s3Paths`에서 텍스트를 추출하여 `=== Document: 파일명 ===` 형식의 헤더로 나눈 다음 컨텍스트로 제공합니다.
  - `llm_server/llm_client.py` 내의 `MOCK_RESPONSE`와 LLM `system_prompt` 지침서에 `evidences` 배열을 명시하여 각 테스트 케이스에 연계된 `chunkId`, `evidenceText`, `sourceName`, `sourceSection` 데이터가 함께 회신되도록 규격을 강화하였습니다.
  - `llm_server/result_formatter.py`에 Fallback 대응 로직을 추가하여, LLM 응답에 `evidences` 필드가 누락되더라도 시나리오 기반의 기본 evidence 정보가 자동 매핑되도록 처리하여 오류 안정성을 확보하였습니다.

#### [Task 3.1.2] 백엔드 Webhook 콜백 저장 및 DB 영구 기록 연동 (완료)
* **구현 내용:**
  - `CallbackService.java` 내 `processCallback` 로직에서 Webhook API 호출을 통해 수신된 테스트 케이스별 `evidences` JSON 데이터를 정상 파싱하고 H2 DB의 `evidence` 테이블에 외래키 무결성을 보장하며 물리적으로 `save` 저장함을 보장하였습니다.

---

## 🧪 Step 3 JUnit 통합 테스트 검증 결과

* **테스트 수행 코드:** [Phase6ExtensionsTests.java](file:///c:/capd/yeonam_tester/backend/src/test/java/com/yeonam/tester/Phase6ExtensionsTests.java)
* **테스트 실행 명령:**
  ```bash
  .maven\apache-maven-3.9.6\bin\mvn.cmd clean test -Dtest=Phase6ExtensionsTests
  ```
* **테스트 시나리오 및 결과:**
  - `testWebhookCallbackEvidencePersistence()`:
    1. H2 DB 상에 더미 프로젝트와 WAITING 상태인 분석 작업 레코드 설정.
    2. `evidences` 데이터(파일명, 장/절, 인용구 포함)가 주입된 `AnalysisCallbackRequest` DTO 생성.
    3. `callbackService.processCallback` 메서드를 직접 실행하여 DB 트랜잭션 흐름을 트리거.
    4. 분석 작업이 `COMPLETED`로 변경되었는지와, `evidenceRepository`를 직접 쿼리하여 chunkId(`CHK-001`), sourceName(`요구사항_명세서.md`), sourceSection(`3.2 사용자 인증 및 보안 설정`), evidenceText가 정확히 일치하여 DB에 영구 기록되었는지 검증.
  - **테스트 결과:** **`BUILD SUCCESS`** (`Tests run: 6, Failures: 0, Errors: 0, Skipped: 0`)

---

## 📘 Step 3 사용자 테스트 가이드

실제 사용자가 이 단계를 직접 확인하고 동작을 테스트할 수 있는 단계별 가이드입니다.

### 1. 웹 브라우저 분석 실행 후 DB Evidence 영구 저장 확인 (E2E)
1. 프론트엔드 React 개발 서버(`http://localhost:5173`), 백엔드 서버(8080 포트) 및 Local S3/MinIO를 실행합니다.
2. `http://localhost:5173/upload-demo` 페이지에서 분석할 기획서 문서를 1개 이상 업로드하고 **샌드박스 데모 모드**를 활성화(또는 LLM 서버를 구동)한 채 '분석 시작'을 가동합니다.
3. 분석 상태 폴링이 완료되고 테스트 케이스 시각화 페이지(`http://localhost:5173/analysis-demo`)로 정상 전환됨을 확인합니다.
4. H2 DB 콘솔(`http://localhost:8080/h2-console`)에 접속합니다.
5. 아래 쿼리를 입력하고 실행합니다:
   ```sql
   SELECT * FROM EVIDENCE;
   ```
6. 각 테스트 케이스에 링크된 근거의 고유키(`CHUNK_ID`), 명세서 실물 파일명(`SOURCE_NAME`), 구체적인 인용 영역(`SOURCE_SECTION`), 그리고 추출된 명세 구절 원문(`EVIDENCE_TEXT`)이 H2 데이터베이스 테이블에 깔끔하게 나누어져 영구 저장되어 있는지 확인합니다.

---

## 📅 Step 4: 테스트 케이스 형태 개선 및 영구 저장 관리 UI 이식 - 완료

### 🛠️ 구현된 테스크 상세 목록

#### [Task 4.1.1] 백엔드 관리 API 수립 및 CRUD 서비스 로직 연동 (완료)
* **구현 내용:**
  - `TestCaseUpdateRequest.java` DTO를 수동 빌더 및 getter/setter 형태로 신규 생성하였습니다.
  - `TestCaseRepository.java`에 프로젝트 ID 기준 JPQL 조회 메서드 `findByProjectId`를 추가하였습니다.
  - `TestCaseService.java`에 프로젝트별 테스트 케이스를 배치 페칭하는 `getTestCasesByProject` 메서드와 특정 필드를 업데이트하는 `updateTestCase` 메서드를 구현하였습니다.
  - 연쇄 파기 제약조건 문제를 해결하기 위해, `TestCase` 삭제 시 연관된 `Evidence`와 `RiskItem`을 먼저 수동 삭제한 후 테스트 케이스를 영구 삭제하는 `deleteTestCase` 메서드를 구현하여 무결성을 보장했습니다.
  - `TestCaseController.java`에 GET /api/projects/{projectId}/testcases, PUT /api/testcases/{testCaseId}, DELETE /api/testcases/{testCaseId} 의 3종 REST 엔드포인트를 구현 완료했습니다.

#### [Task 4.1.2] 프론트엔드 관리 UI 보드 및 API 연동 (완료)
* **구현 내용:**
  - `api.ts`에 `testCaseApi` Axios CRUD 호출 함수들을 추가하여 API 연동 규격을 완성했습니다.
  - `DashboardPage.tsx`에 "테스트 케이스 관리 보드" 탭을 추가하고, 해당 프로젝트에 영구 저장된 전체 테스트 케이스 리스트가 렌더링되도록 하였습니다.
  - 리스트 내의 각 항목에 인라인 수정 모달 팝업을 연계하여 사용자가 텍스트를 손쉽게 편집하고 영구 반영할 수 있도록 UI를 이식하였습니다.
  - 개별 테스트 케이스 삭제 버튼을 추가하고, 클릭 시 즉각적인 삭제 API가 가동되어 목록 및 DB에서 완전히 지워지도록 핸들러를 연동했습니다.
  - `AnalysisResultPage.tsx`에서 테스트 케이스 카드 하단에 출처(Evidence) 배지 UI와 상세 인용 텍스트를 시각화하였습니다.

---

## 🧪 Step 4 JUnit 통합 테스트 및 빌드 검증 결과

* **테스트 수행 코드:** [Phase6ExtensionsTests.java](file:///c:/capd/yeonam_tester/backend/src/test/java/com/yeonam/tester/Phase6ExtensionsTests.java)
* **테스트 실행 명령:**
  ```bash
  .maven\apache-maven-3.9.6\bin\mvn.cmd clean test -Dtest=Phase6ExtensionsTests
  ```
* **백엔드 테스트 결과:** **`BUILD SUCCESS`** (`Tests run: 7, Failures: 0, Errors: 0, Skipped: 0` - 신규 `testTestCaseCRUD` 성공적 통과)
* **프론트엔드 빌드 실행 명령:**
  ```bash
  npm run build
  ```
* **프론트엔드 빌드 결과:** **`BUILD SUCCESS`** (`tsc && vite build` 컴파일 및 번들링 무결점 완료)

---

## 📘 Step 4 사용자 테스트 가이드

실제 사용자가 이 단계를 직접 확인하고 동작을 테스트할 수 있는 단계별 가이드입니다.

### 1. 테스트 케이스 수정/삭제 보드 테스트 (E2E)
1. 프론트엔드 React 개발 서버(`http://localhost:5173`)와 백엔드 서버(8080 포트)를 실행합니다.
2. `http://localhost:5173/dashboard` 페이지로 이동합니다.
3. 프로젝트 목록 하단의 대시보드 메인 뷰에서 **'테스트 케이스 관리 보드'** 탭을 클릭하여 이동합니다.
4. 기존에 생성 완료된 테스트 케이스 리스트가 깔끔한 테이블 형태로 노출되는지 확인합니다.
5. 변경하고자 하는 테스트 케이스 행의 **'수정'** 아이콘(또는 버튼)을 클릭합니다.
6. 팝업된 모달 창에서 시나리오명, 사전 조건, 기대 결과 텍스트 등을 일부 편집한 후 **'저장'**을 누릅니다.
7. 페이지를 새로고침하여 수정한 텍스트 내용이 목록에 영구적으로 반영되어 렌더링되는지 확인합니다.
8. 해당 행의 **'삭제'** 버튼을 클릭하여 테스트 케이스가 목록에서 즉각 소멸하는지 확인합니다.
9. H2 DB 콘솔(`http://localhost:8080/h2-console`)에서 `SELECT * FROM TEST_CASE;` 및 `SELECT * FROM EVIDENCE;` 조회를 수행해 삭제된 테스트 케이스와 연관된 Evidence 자식 레코드가 외래키 오류 없이 완전 제거되었는지 데이터 정합성을 검증합니다.

---

## 📅 Step 5: 테스트 케이스 선택형 보고서 생성 아키텍처 개편 - 완료

### 🛠️ 구현된 테스크 상세 목록

#### [Task 5.1.1] 중간 조인 테이블 및 DB 매핑용 ReportTestCase 엔티티 신설 (완료)
* **구현 내용:**
  - `ReportTestCase.java` 엔티티 클래스를 수동 getter/setter 및 Builder 패턴으로 신설했습니다.
  - `ReportTestCaseRepository.java` 리포지토리를 추가하여 JpaRepository 인터페이스를 연동했습니다.
  - `Report` 엔티티(`Report.java`) 내에 `reportTestCases` 일대다 관계를 맺어 양방향 연관관계를 수립했습니다.
  - H2 데이터베이스의 `schema.sql` 파일에 N:M 매핑 테이블인 `report_test_case` 테이블을 생성하는 DDL 구문과 DROP TABLE 구문을 추가하여 스키마 정합성을 보장했습니다.

#### [Task 5.1.2] API DTO 개편 및 서비스 조립 로직 변경 (완료)
* **구현 내용:**
  - `ReportCreateRequest.java` DTO에 `testCaseIds` 필드를 추가하고 수동 Builder 및 getter/setter에 반영했습니다.
  - `ReportAssemblyService.java`에 오버로딩 메서드 `assembleReportData(String analysisId, List<String> testCaseIds)`를 제공하여, `testCaseIds`가 지정된 경우 해당 테스트 케이스 정보만 필터링하여 보고서 데이터 구조를 조립하도록 코드를 전면 개편했습니다.
  - `ReportService.java` 내 `generateReport`에서 API를 통해 선택된 테스트 케이스들만 마크다운 또는 PDF로 컴파일하고, H2 DB에 `ReportTestCase` 매핑 레코드를 함께 `save`하여 영구 조인 정보를 저장하도록 구현을 완료했습니다.

#### [Task 5.1.3] 프론트엔드 체크박스 보고서 생성 연동 (완료)
* **구현 내용:**
  - `api.ts` 내의 `reportApi.generate` 메서드가 `testCaseIds` 배열을 추가 인자로 받아서 백엔드 body payload에 실어 호출할 수 있도록 시그니처를 확장하였습니다.
  - `AnalysisResultPage.tsx`에서 각 테스트 케이스 카드 좌측에 **체크박스**를 이식하고, 카드 클릭이나 체크박스 토글을 통해 선택된 케이스 ID들을 `selectedTestCaseIds` React 상태 배열에서 추적하도록 개선했습니다.
  - 상단 갯수 표시 우측에 **'전체 선택'** 체크박스를 도입하여 다수의 테스트 케이스를 일괄 토글할 수 있게 기능을 강화하였습니다.
  - 하단의 보고서 생성 버튼 액션 클릭 시, `selectedTestCaseIds` 배열을 payload에 전달하여 지정한 테스트 케이스들만 수록된 커스텀 보고서를 가동할 수 있도록 연동을 완료했습니다.
  - 선택된 테스트 케이스가 0개인 상태에서 보고서 생성을 가동할 시, 예외 경고 메시지를 표출하여 무효한 생성을 차단하도록 조치했습니다.

---

## 🧪 Step 5 JUnit 통합 테스트 및 빌드 검증 결과

* **테스트 수행 코드:** [Phase6ExtensionsTests.java](file:///c:/capd/yeonam_tester/backend/src/test/java/com/yeonam/tester/Phase6ExtensionsTests.java)
* **테스트 실행 명령:**
  ```bash
  .maven\apache-maven-3.9.6\bin\mvn.cmd clean test -Dtest=Phase6ExtensionsTests
  ```
* **백엔드 테스트 결과:** **`BUILD SUCCESS`** (`Tests run: 8, Failures: 0, Errors: 0, Skipped: 0` - 신규 `testGenerateReportWithSelectedTestCases` 성공적 통과)
* **프론트엔드 빌드 실행 명령:**
  ```bash
  npm run build
  ```
* **프론트엔드 빌드 결과:** **`BUILD SUCCESS`** (`tsc && vite build` 컴파일 및 번들링 무결점 완료)

---

## 📘 Step 5 사용자 테스트 가이드

실제 사용자가 이 단계를 직접 확인하고 동작을 테스트할 수 있는 단계별 가이드입니다.

### 1. 웹 브라우저 선택형 보고서 생성 검증 (E2E)
1. 프론트엔드 React 개발 서버(`http://localhost:5173`)와 백엔드 서버(8080 포트)를 가동합니다.
2. 분석 실행을 통해 신규 분석 결과 페이지(`http://localhost:5173/analysis-demo?analysisId=ANA-xxx`)로 진입합니다.
3. 생성된 테스트 케이스 카드들 중 **일부(예: 1개만) 체크박스를 선택**하고, 나머지는 체크를 해제합니다.
4. 하단으로 스크롤하여 **'선택된 테스트 케이스로 보고서 생성'** 버튼을 클릭합니다.
5. 팝업 창에서 포맷(예: Markdown)을 선택하고 **'보고서 생성 실행'**을 클릭합니다.
6. 자동으로 전환된 보고서 미리보기 화면(`http://localhost:5173/report-demo?reportId=RPT-xxx`)에서 **내가 선택했던 그 1개의 테스트 케이스 내용만** 보고서 마크다운에 수록되어 있는지 스크롤하며 눈으로 확인합니다.
7. H2 DB 콘솔(`http://localhost:8080/h2-console`)에 접속하여 `SELECT * FROM REPORT_TEST_CASE;` 쿼리를 실행해 매핑 레코드가 내가 선택한 개수만큼 올바르게 등록되어 작동하고 있는지 최종 정합성을 검증합니다.

---

## 📅 Step 6: 파일을 통한 보고서 필터링 (조인 쿼리 구현) - 완료

### 🛠️ 구현된 테스크 상세 목록

#### [Task 6.1.1] TestCase-Evidence 양방향 매핑 추가 (완료)
* **구현 내용:**
  - `TestCase.java` 엔티티 내에 `evidences` 일대다(`@OneToMany`) 양방향 연관관계를 수립하고 수동 getter/setter를 이식하여 JPQL에서 `TestCase`를 거쳐 `Evidence`로 자연스럽게 조인할 수 있도록 개선하였습니다.

#### [Task 6.1.2] JPQL 조인 및 파일 필터링 쿼리 보정 (완료)
* **구현 내용:**
  - `ReportRepository.java` 내 `findByProjectWithFilters` 메서드의 JPQL을 고도화하여, `Report r` -> `ReportTestCase rtc` -> `TestCase tc` -> `Evidence e`를 조인하고, `e.sourceName = uf.fileName` (ON절 조인)을 거쳐 최종적으로 `UploadedFile uf`까지 조인 경로를 정밀하게 수립했습니다.
  - 기존에 `r.uploadedFile.fileId`를 참조하던 방식과의 하위 호환성을 보장하기 위해 `(:fileId IS NULL OR r.uploadedFile.fileId = :fileId OR uf.fileId = :fileId)` 논리식을 구성하여, 직접 업로드된 파일 매칭 보고서와 Evidence를 통해 기획서 조인이 된 보고서 모두가 올바르게 필터링될 수 있도록 정합성을 완벽히 만족시켰습니다.

---

## 🧪 Step 6 JUnit 통합 테스트 및 빌드 검증 결과

* **테스트 수행 코드:** [Phase6ExtensionsTests.java](file:///c:/capd/yeonam_tester/backend/src/test/java/com/yeonam/tester/Phase6ExtensionsTests.java)
* **테스트 실행 명령:**
  ```bash
  .maven\apache-maven-3.9.6\bin\mvn.cmd clean test -Dtest=Phase6ExtensionsTests
  ```
* **백엔드 테스트 결과:** **`BUILD SUCCESS`** (`Tests run: 8, Failures: 0, Errors: 0, Skipped: 0` - 전체 필터링 정합성 테스트 무결점 통과)
* **프론트엔드 빌드 실행 명령:**
  ```bash
  npm run build
  ```
* **프론트엔드 빌드 결과:** **`BUILD SUCCESS`** (`tsc && vite build` 컴파일 및 번들링 완료)

---

## 📘 Step 6 사용자 테스트 가이드

실제 사용자가 이 단계를 직접 확인하고 동작을 테스트할 수 있는 단계별 가이드입니다.

### 1. 기획서 문서를 통한 보고서 필터링 테스트 (E2E)
1. 프론트엔드 React 개발 서버(`http://localhost:5173`)와 백엔드 서버(8080 포트)를 실행합니다.
2. 기획 명세서 문서 `A.pdf`와 `B.pdf`를 각각 업로드하고 각각 분석을 진행하여 각각 1건씩 보고서 `RPT-A`, `RPT-B`를 생성합니다.
3. `http://localhost:5173/dashboard` 페이지로 이동합니다.
4. 문서 목록(또는 프로젝트 내 업로드된 파일 목록)에서 문서 `A.pdf` 옆의 **'필터 아이콘'**(깔때기)을 클릭합니다.
5. 대시보드 하단의 '분석 및 보고서 이력' 목록에서 문서 `A.pdf`를 참조하여 만들어진 보고서 `RPT-A`만 필터링되어 화면에 노출되고, `B.pdf` 관련 보고서인 `RPT-B`는 화면에서 즉시 배제되는지 시각적으로 검증합니다.
6. H2 DB 콘솔(`http://localhost:8080/h2-console`)에서 `SELECT * FROM REPORT r LEFT JOIN REPORT_TEST_CASE rtc ON r.REPORT_ID = rtc.REPORT_ID ...` 조인을 수행하여 필터링의 JPQL 조인 경로가 데이터베이스의 매핑 튜플들과 정확히 일치하여 정상 반환되고 있는지 대조해 봅니다.

---

## 📅 Step 7: 보고서 반출 포맷 실시간 변환(On-the-fly) 아키텍처 구축 - 완료

### 🛠️ 구현된 테스크 상세 목록

#### [Task 7.1.1] S3 원본 캐시 단일 마크다운화 (완료)
* **구현 내용:**
  - `ReportService.java` 내의 `generateReport` 메서드를 수정하여, 클라이언트가 요청한 최종 포맷에 관계없이 S3(reportsBucket)에는 항상 확장자 `.md` 및 `text/markdown` 타입의 단일 표준 마크다운 파일만 업로드하여 보관하도록 구조를 최적화했습니다. DB 레코드의 포맷 컬럼도 `MARKDOWN`으로 단일 저장합니다.

#### [Task 7.1.2] 실시간 포맷 변환(On-the-fly) 다운로드 로직 및 컨트롤러 연동 (완료)
* **구현 내용:**
  - `ReportDownloadService.java`에 `downloadReportBytes(String reportId, String format)` 오버로딩 메서드를 추가하여 실시간 포맷 변환 아키텍처를 이식했습니다.
  - S3에 적재된 표준 마크다운 파일을 로드한 후, 요청된 포맷이 `PDF`인 경우 즉석에서 `renderEngine.renderPdf(markdown)`를 호출해 PDF 바이너리를 생성하여 반환하도록 구현을 완료했습니다.
  - `ReportController.java`의 다운로드 엔드포인트 `/api/reports/{reportId}/download`에 `@RequestParam(required = false) String format`을 바인딩받고, 요청 포맷에 따라 파일 확장자(`.pdf` / `.md`) 및 MIME 타입(`application/pdf` / `text/markdown`)을 실시간으로 분기 매핑하여 HTTP 응답 스트림으로 전송하도록 처리하였습니다.

#### [Task 7.1.3] 프론트엔드 포맷 선택 다운로드 드롭다운 메뉴 배치 (완료)
* **구현 내용:**
  - `api.ts` 내의 `reportApi.download` URL 생성 함수가 format 인자를 쿼리 파라미터(`?format=...`)로 포함하여 주소를 빌드하도록 기능을 확장했습니다.
  - `DashboardPage.tsx`에서 보고서 이력 테이블의 기존 다운로드 버튼을 **드롭다운 메뉴**(`expand_more` 아이콘 결합) 형태로 전면 개편했습니다.
  - 다운로드 클릭 시 **'Markdown (.md)'**과 **'PDF (.pdf)'** 2가지 선택지를 렌더링하고, 바깥 영역을 클릭 시 드롭다운이 자동으로 닫히도록 리액트 외부 클릭 리스너를 연계하여 프리미엄 UX를 완성했습니다.

---

## 🧪 Step 7 JUnit 통합 테스트 및 빌드 검증 결과

* **테스트 수행 코드:** [Phase6ExtensionsTests.java](file:///c:/capd/yeonam_tester/backend/src/test/java/com/yeonam/tester/Phase6ExtensionsTests.java)
* **테스트 실행 명령:**
  ```bash
  .maven\apache-maven-3.9.6\bin\mvn.cmd clean test -Dtest=Phase6ExtensionsTests
  ```
* **백엔드 테스트 결과:** **`BUILD SUCCESS`** (`Tests run: 9, Failures: 0, Errors: 0, Skipped: 0` - 신규 `testDownloadOnTheFlyPdf`를 포함한 9건의 검증 완벽 통과)
* **프론트엔드 빌드 실행 명령:**
  ```bash
  npm run build
  ```
* **프론트엔드 빌드 결과:** **`BUILD SUCCESS`** (`tsc && vite build` 컴파일 및 번들링 무결점 완료)

---

## 📘 Step 7 사용자 테스트 가이드

실제 사용자가 이 단계를 직접 확인하고 동작을 테스트할 수 있는 단계별 가이드입니다.

### 1. 실시간 온더플라이 보고서 다운로드 E2E 검증
1. 프론트엔드 React 개발 서버(`http://localhost:5173`)와 백엔드 서버(8080 포트)를 실행합니다.
2. `http://localhost:5173/dashboard` 페이지로 이동합니다.
3. 분석 및 보고서 이력 탭의 목록에서 특정 보고서의 **'다운로드'** 버튼을 클릭합니다.
4. 펼쳐진 드롭다운 메뉴에서 **'PDF (.pdf)'**를 선택합니다.
5. 로컬 환경에 `.pdf` 파일로 보고서가 즉시 반출 완료되며, 실행 시 깨짐 없는 정밀 변환 렌더링을 확인합니다.
6. 이어서 동일한 보고서 행의 다운로드 메뉴에서 **'Markdown (.md)'**을 선택해 `.md` 텍스트 원본이 정상 다운로드되는지 비교 검증합니다.
7. 로컬 캐시 디렉터리나 S3 저장공간을 추적하여, 해당 보고서 ID 아래에는 **포맷 구분 없이 단 1개의 `.md` 캐시 파일만 존재**함을 대조하여 물리 디스크 낭비 방지를 검증합니다.
